---
title: The title of the investigation
date: 2026-09-19
summary: One or two sentences for the index, the RSS feed and the email preview.
draft: true
---

This file is a template: files whose names start with an underscore are never
published. Copy it to a new file, for example `2026-10-port-contracts.md`. The
file name becomes the address: `/investigations/2026-10-port-contracts`.

Write in Markdown. Headings (`##`), lists, quotes, links, tables and code all
work, and the site's writing rules apply: no em dashes, no exclamation marks.

## Sources

Link every source you rely on, and say what each one shows.

Set `draft: true` while you work (drafts show only with `npm run dev`), and
remove it to publish. Then send it to subscribers with
`npm run newsletter -- send <slug>`.
